interface:
  display_name: "Unslop"
  short_description: "Remove AI tells from writing"
  default_prompt: "Use $unslop to remove AI-writing tells while preserving meaning and tone."
policy:
  allow_implicit_invocation: true
