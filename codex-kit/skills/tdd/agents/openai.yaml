interface:
  display_name: "TDD"
  short_description: "Fix a bug through a focused failing test"
  default_prompt: "Use $tdd to reproduce this bug with a focused failing test before fixing it."
policy:
  allow_implicit_invocation: false
